{% extends 'base.html.twig' %}

{% block title %}Projet
{% endblock %}

{% block body %}
	<h1>Projet</h1>

	<table class="table table-bordered">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ projet.id }}</td>
			</tr>
			<tr>
				<th>Nom du projet</th>
				<td>{{ projet.nom }}</td>
			</tr>
			<tr>
				<th>Chef de projet</th>
				<td>{{ projet.chefprojet.username }}</td>
			</tr>
			<tr>
				<th>Porteur de projet</th>
				<td>{{ projet.porteur.username }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_projet_index') }}" class="btn btn-light">Revenir à liste</a>

	<a href="{{ path('app_projet_edit', {'id': projet.id}) }}" class="btn btn-secondary">Modifier</a>

	{# {{ include('projet/_delete_form.html.twig') }} #}
{% endblock %}
