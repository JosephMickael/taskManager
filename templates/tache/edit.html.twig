{% extends 'base.html.twig' %}

{% block title %}Modifer tache
{% endblock %}

{% block body %}
	<div class="px-3">
		<p class="fs-3 mb-4" style="margin-top: -30px">Modifier la
			<b>Tache</b>
		</p>
	</div>

	{{ include('tache/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

	<div class="px-3 mb-3">
		{{ include('tache/_delete_form.html.twig') }}
	</div>

	<div class="mb-5">
		<a href="{{ path('app_tache_index') }}" class="position-relative" style="font-size: .9em; text-decoration: none; left: 10px">
			<i class="bi bi-arrow-left-circle position-relative" style="top: -3px"></i>
			Revenir à la liste
		</a>
	</div>
{% endblock %}
