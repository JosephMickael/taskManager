{% extends 'base.html.twig' %}

{% block title %}Edit Ticket
{% endblock %}

{% block body %}
	<p class="fs-3 mb-4 ms-2" style="margin-top: -30px">Modifier /
		<b>Ticket</b>
	</p>

	<div class="px-2">
		{{ include('ticket/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
		{{ include('ticket/_delete_form.html.twig') }}
	</div>

	<a href="{{ path('app_ticket_index') }}" class="position-relative" style="text-decoration: none; left: 2px; font-size: .9em">
		<span>
			<i class="bi bi-arrow-left-circle position-relative" style="top: -2px"></i>
			Revenir à la liste
		</span>
	</a>

{% endblock %}
